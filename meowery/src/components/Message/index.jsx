
function Message({ cards }) {

// Add a check to see if cards is defined
  // const matchedCards = cards ? cards.filter((card) => card.matched) : [];

//   if (matchedCards.length === cards.length) {
//     return <div>Good work! All cats have found their buddy!</div>;
//   } else if (matchedCards.length > 1) {
//     return (
//       <div>
//         It's a match!
//       </div>
//     )
//   } else {
    return ( 
      <div></div>   
  );
  }

// }

export default Message;
